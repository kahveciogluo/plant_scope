import '../../../../app_export.dart';

class IdentifyPage extends StatelessWidget {
  const IdentifyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Identify', style: context.textTheme.headlineMedium),
      ),
    );
  }
}
